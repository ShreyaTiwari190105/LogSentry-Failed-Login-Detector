# LogSentry-Failed-Login-Detector
Python-based log analysis tool to detect failed SSH login attempts and identify potential brute-force attacks.

## Features

- Reads Linux authentication logs

- Displays total log entries

- Prints log entries sequentially
